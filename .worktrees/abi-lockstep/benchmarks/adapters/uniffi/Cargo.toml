[package]
name = "bench_uniffi_tools"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "uniffi-bindgen"
path = "uniffi-bindgen.rs"

[dependencies]
uniffi = { version = "0.32", features = ["cli"] }

# Empty table: standalone workspace root, same nested-git-worktree fix as examples/demo/Cargo.toml
# and runtime/typescript/test/fixtures/rn_poc/Cargo.toml -- see those for the full rationale.
[workspace]
